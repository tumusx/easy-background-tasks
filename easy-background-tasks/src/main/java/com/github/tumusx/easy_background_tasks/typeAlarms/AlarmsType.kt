package com.github.tumusx.easy_background_tasks.typeAlarms

enum class AlarmsType{
    SET,
    SET_EXACT,
    SET_AND_ALLOW_WHILE_IDLE,
    SET_EXACT_AND_ALLOW_WHILE_IDLE,
    SET_WINDOW,
    SET_ALARM_CLOCK,
}